/**
 * Re-exporta el WorkflowService desde el módulo global sistema/flujos.
 * Mantiene compatibilidad con imports existentes en otros componentes.
 */
export { WorkflowService } from '../../modules/sistema/flujos/services/workflow.service';
